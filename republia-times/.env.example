# JSONBin.io configuration for remote article loading.
# Create a READ-ONLY access key on jsonbin.io (not the master key) for production builds.
# These are baked into the bundle at build time by Vite.
VITE_JSONBIN_KEY=
VITE_JSONBIN_BIN=
